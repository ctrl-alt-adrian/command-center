node_modules/
.svelte-kit/
build/
dist/
.DS_Store

# Local env
.env
.env.local
.env.*.local
!.env.example

# Runtime data — generated by the pipelines, not source
tasks/*
!tasks/.gitkeep
signals/*
!signals/.gitkeep
drafts/*
!drafts/.gitkeep
logs/*
!logs/.gitkeep

# Vault content — phase 3 captures structure but personal notes stay local
vault/*
!vault/.gitkeep
!vault/README.md

# Marketing pipeline runtime
pipelines/marketing/config.json

# RoleNext bug-resolver runtime state — fingerprints + daily counter regenerate per machine
pipelines/rolenext/bug-resolver/state/*.json
