# scaffold-core-processor

Scaffold command-center repo and the domain-agnostic core: pipeline DAG runtime, three gate types, task store, processor loop with backpressure cap (max 5 needs_review tasks per pipeline). Foundation for porting marketing-pipeline and adding new domains.
