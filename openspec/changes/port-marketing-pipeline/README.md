# port-marketing-pipeline

Port existing marketing-pipeline (SvelteKit + claude -p) into command-center as pipelines/marketing consuming core. Preserves discovery→generate→slop-check→review DAG, slop gate, platform fan-out, drafts editor.
